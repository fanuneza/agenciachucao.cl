{
  "name": "agenciachucao-cl",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test:lighthouse": "lhci autorun",
    "capture:local": "playwright test --config playwright.config.ts",
    "capture:home": "playwright test --config playwright.config.ts --grep @home",
    "capture:blog": "playwright test --config playwright.config.ts --grep @blog",
    "capture:servicios": "playwright test --config playwright.config.ts --grep @servicios",
    "capture:portafolio": "playwright test --config playwright.config.ts --grep @portafolio"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "@fontsource/lora": "^5.2.8",
    "@fontsource/montserrat": "^5.2.8",
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.59.1"
  }
}
