---
interface Props {
  included: string[];
  excluded: string[];
  extras: string[];
}

const { included, excluded, extras } = Astro.props;
---

<section class="section scope-section" id="alcance" aria-labelledby="inc-h2">
  <div class="wrap">
    <div class="tray">
      <span class="serial">§ 04 / Alcance</span>
      <span class="meta">Lo que incluye y lo que no</span>
    </div>

    <h2 id="inc-h2">Qué incluye y qué no.</h2>
    <p class="lede" style="margin-top:1.5rem;">
      Un alcance acotado es lo que permite ejecutar cada pieza bien. Aquí está todo lo que incluye el fee mensual, y lo
      que no, para que no haya sorpresas.
    </p>

    <div class="scope-section__grid">
      <div class="scope-section__col scope-section__col--included">
        <p class="scope-section__col-label">Sí incluye</p>
        <ul class="scope-section__list" role="list">
          {
            included.map((item) => (
              <li>
                <span class="scope-section__list-mark" aria-hidden="true">
                  <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                    <path
                      d="M2 7l4 4 6-6"
                      stroke="currentColor"
                      stroke-width="1.5"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                  </svg>
                </span>
                <span>{item}</span>
              </li>
            ))
          }
        </ul>
      </div>
      <div class="scope-section__col scope-section__col--excluded">
        <p class="scope-section__col-label">No incluye</p>
        <ul class="scope-section__list" role="list">
          {
            excluded.map((item) => (
              <li>
                <span class="scope-section__list-mark" aria-hidden="true">
                  <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                    <path d="M3 7h8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
                  </svg>
                </span>
                <span>{item}</span>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    {
      extras.length > 0 && (
        <div class="scope-section__extras">
          <p class="scope-section__extras-label">Servicios opcionales</p>
          <ul class="scope-section__extras-list" role="list">
            {extras.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </div>
      )
    }

    <p class="note scope-section__note">
      La pauta publicitaria siempre va aparte. El cliente la paga directamente a Google. Nunca la incluimos en el fee.
      Así mantenemos las cuentas en manos del cliente y sin opacidad.
    </p>
  </div>
</section>

<style>
  .scope-section__grid {
    margin-top: clamp(2.5rem, 5vw, 4rem);
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
    border: 1px solid var(--bone-line);
  }
  @media (min-width: 900px) {
    .scope-section__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .scope-section__col {
    padding: clamp(1.5rem, 2.5vw, 2.25rem);
  }
  .scope-section__col + .scope-section__col {
    border-top: 1px solid var(--bone-line);
  }
  @media (min-width: 900px) {
    .scope-section__col + .scope-section__col {
      border-top: 0;
      border-left: 1px solid var(--bone-line);
    }
  }

  .scope-section__col-label {
    font-family: var(--f-display);
    font-size: var(--t-lg);
    font-weight: 600;
    letter-spacing: -0.01em;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding-bottom: 1rem;
    margin-bottom: 1.25rem;
    border-bottom: 1px solid var(--bone-line);
    width: 100%;
  }
  .scope-section__col--included .scope-section__col-label {
    color: var(--ink);
  }
  .scope-section__col--included .scope-section__col-label::before {
    content: "";
    width: 8px;
    height: 8px;
    background: var(--vermilion);
    display: inline-block;
    border-radius: 50%;
  }
  .scope-section__col--excluded .scope-section__col-label {
    color: var(--muted);
  }
  .scope-section__col--excluded .scope-section__col-label::before {
    content: "";
    width: 8px;
    height: 8px;
    border: 1.5px solid var(--muted);
    display: inline-block;
    border-radius: 50%;
  }

  .scope-section__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0;
  }
  .scope-section__list li {
    padding: 0.9rem 0;
    border-bottom: 1px solid var(--bone-line);
    display: grid;
    grid-template-columns: 22px 1fr;
    gap: 0.85rem;
    font-size: var(--t-md);
    line-height: 1.5;
    align-items: start;
  }
  .scope-section__list li:last-child {
    border-bottom: 0;
  }
  .scope-section__col--excluded .scope-section__list li {
    color: var(--muted);
  }

  .scope-section__list-mark {
    color: var(--vermilion);
    margin-top: 0.15em;
    display: inline-flex;
  }
  .scope-section__col--excluded .scope-section__list li .scope-section__list-mark {
    color: var(--muted);
  }

  .scope-section__extras {
    margin-top: clamp(2rem, 3vw, 2.5rem);
    padding: clamp(1.25rem, 2vw, 1.75rem);
    border: 1px solid var(--bone-line);
    background: var(--bone-2);
  }

  .scope-section__extras-label {
    font-family: var(--f-mono);
    font-size: var(--t-xs);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--graphite);
    margin: 0 0 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--bone-line);
  }

  .scope-section__extras-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.5rem;
  }
  @media (min-width: 640px) {
    .scope-section__extras-list {
      grid-template-columns: 1fr 1fr;
    }
  }
  .scope-section__extras-list li {
    font-size: var(--t-sm);
    color: var(--graphite);
    line-height: 1.5;
    padding-left: 1rem;
    position: relative;
  }
  .scope-section__extras-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.55em;
    width: 5px;
    height: 1px;
    background: var(--muted);
  }

  .scope-section__note {
    margin-top: 1.5rem;
    max-width: 70ch;
  }
</style>
